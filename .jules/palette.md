## 2026-06-12 - Actionable Error States
**Learning:** In a text-based Discord UI, users lack visual cues for hidden state bounds (like context limits). Without buttons, they get stuck in error loops.
**Action:** Always surface actionable recovery commands (like `!clear`) directly within error messages to provide an explicit escape hatch.
